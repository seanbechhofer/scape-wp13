div do
  p "This application provides a simple front end for creating SCAPE control policies/objectives"
end

h2 "Criterion Categories"

div do
  ul do |ul|
    categories.each do |category|
      ul.li {
        category[:category].to_s + " " + category[:label].to_s
      }
    end
  end
end

